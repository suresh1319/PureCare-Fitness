<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title || 'PureCare Gym' %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        html,
        body {
            min-height: 100vh;
            width: 100%;
            max-width: 100%;
            overflow-x: hidden;
        }

        body {
            width: 100%;
            max-width: 100%;
            display: flex;
            flex-direction: column;

        }

        .main-content {
            width: 100vw;
            margin: 0;
            padding: 0px;
            flex: 1;
            /* background-image: url('/images/servicebg.webp');
            background-position: center;
            background-size: cover; */

        }

        /* Add styles for pages without navbar */
        /* body.no-navbar .main-content {
            padding-top: 20px;
        } */

        /* Add padding for fixed navbar */
        /* body:not(.no-navbar) .main-content {
            padding-top: 20px;
        } */

        footer {
            background: rgba(0, 0, 0, 0.8);
            width: 100%;
        }
    </style>
    <%- typeof style !=='undefined' ? style : '' %>
</head>

<body class="<%= typeof noNavbar !== 'undefined' && noNavbar ? 'no-navbar' : '' %>">
    <% if (typeof noNavbar==='undefined' || !noNavbar) { %>
        <%- include('partials/navbar') %>
            <% } %>

                <div class="main-content">
                    <%- typeof body !=='undefined' ? body : '' %>
                </div>
                <% if (typeof noFooter==='undefined' || !noNavbar) { %>
                    <%- include('partials/footer') %>
                        <% } %>
                            <%- typeof script !=='undefined' ? script : '' %>
</body>

</html>